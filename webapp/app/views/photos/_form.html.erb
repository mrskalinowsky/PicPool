<%= form_for [@pool, @photo], :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :description, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :description, :class => 'text_field' %>
    </div>
  </div>
  
    <%= image_tag @photo.image.large.url, :id => 'cropbox' %>
  
  <% %w[x y w h].each do |attribute| %>
    <%= f.hidden_field "crop_#{attribute}" %>
  <% end %>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary ' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                pool_path(@pool), :class => 'btn' %>
    <%= link_to 'Default', '', :class => 'btn btn-success' , :id => 'make_default'%>

  </div>
<% end %>

<script type="text/javascript" charset="utf-8">
    $(function () {
      $('#make_default').click(function(event) {
        event.preventDefault();
        $.ajax({
          url: '<%= make_default_pool_photos_path %>',
          type: 'POST',
          data: {id : <%= @photo.id %>, pool_id : <%= @pool.id %>}
        });
      });
    });
</script>